rsconnect
renv/renv_location.R
/env
/data
/_bookdown_files
/.quarto/
CNAME
.Rproj.user
.Rhistory
.Renviron
.env
*.Rproj
*.db
*.pdf
*.rds
*.duckdb
*.sqlite
*.rdb
*.RData
*.rdx
*.parquet
*.quarto_ipynb
macro_predictors.xlsx

**/*.quarto_ipynb
config.yaml
.claude/

# Quarto/knitr in-place render artifacts for the unified chapters
/chapters/*_cache/
/chapters/*_files/

# Stray source .qmd that Quarto copies into the output dir for the legacy
# archive hub post. If committed it is re-discovered as a project input and
# breaks `quarto render` (it gets cleaned mid-render, then read fails).
/docs/blog/legacy-python-pandas/index.qmd
